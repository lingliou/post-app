// lightTheme.js
import { createTheme } from "@mui/material/styles";

const lightTheme = createTheme({
    palette: {
        primary: {
            main: "#2196F3",
        },
        mode: "light",
    },
});

export default lightTheme;
